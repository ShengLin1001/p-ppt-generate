---
name: p-ppt-academic
description: "Use ONLY when the user explicitly asks to build, edit, or review an academic .pptx deck with this skill — thesis defense, 组会汇报, 开题/中期, 项目答辩, 会议/论坛报告. Do NOT invoke on your own just because a .pptx is involved or for non-academic slides. 触发：/p-ppt-academic、$p-ppt-academic、用 p-ppt-academic 做答辩/组会/论坛 PPT、按浙大力学所模板出 PPT、用这个 skill 改汇报 PPT 的某页。配合 p-code-style 写脚本。"
---

# P Academic PPT

给 PJ 做学术汇报 deck。视觉基准 = `assets/reference-deck.pptx`（从一份浙大应用力学所的
博士答辩 deck 抽出、已匿名化的 10 页版式骨架），叙事基准 = Assertion–Evidence 的中式答辩变体。

## 触发条件

只在用户显式调用时执行：Claude Code 输入 `/p-ppt-academic`，Codex 写 `$p-ppt-academic`。
单纯读取或转换 .pptx、非学术用途的幻灯片，不走本流程。

**从零做新 deck 走全流程（§1–§7）。** 改已有 deck 时跳过克隆，直接用 officecli 改（§4、§6），
改完仍过 §5 质检门。审阅或只做美化时，用 §5 质检门 + §3 单页法则当清单。

## 铁律

1. **模板管风格，不管尺寸**：标题栏、配色、字体、字号阶梯、旗标、页码徽章一律从参考 deck 克隆，
   不另行设计、不加装饰。面板和图的尺寸按图来定（§3.2），参考 deck 的面板几何只是起点——
   样板匿名化时图被删光了，照搬空框的尺寸塞图，两侧必然留白。参考 deck 只读，要改先复制。
2. **放不下就删文字或拆页，绝不缩字号**。字号阶梯是视觉层级本身，破了层级整页就散。
3. **每页有且只有一句断言句，只出现一次**。没有断言句的页 = 没想清楚要证明什么，退回重写，
   不要先排版；同一结论换个说法再写一遍（页底 ✓ 复述顶幅）只会挤掉证据。
4. **不进生图管线**。公式、数据、图注必须可编辑可校对；概念示意图缺失就留占位框标注
   「待补图：<内容>」，不要用文生图糊上去。

## 1. 先定骨架，再碰 PPTX

顺序不能反：**叙事 → 版式计划 → 克隆 → 填内容 → 质检**。先建 PPTX 再想讲什么必返工。

**按时长分档**（样板是 45 min 答辩，短报告照搬会把一半页数花在导航上）：

| 档 | 页数预算 | 骨架 |
|---|---|---|
| 长答辩（≥ 30 min：博士答辩、开题、中期） | 分钟 × 1.2 | 封面 → 目录 → [扉页 → 内容页 × n] × 章 → 总结与展望 → 主要成果 → 致谢 |
| 短报告（≤ 15 min：组会、论坛、会议） | 分钟 × 1.0 | 封面 → 内容页 × n → take-home 总结 → 致谢。**不要目录和扉页**，章节只在标题栏体现 |

介于两者之间的：≥ 4 章才加目录，扉页照样省。超出预算就砍章节，不要靠压缩单页密度。

**Ghost deck 测试**（交付前必跑）：把每页的断言句按顺序抽出来读一遍，应当自成一条完整论证链
——问题 → 为什么难 → 怎么做 → 证据 → 所以呢。读不通就是结构问题，改结构别改排版。

研究背景章内部按 `领域是什么 → 典型应用 → 现有方法 → 现有理论 → 挑战与创新 → 本文内容总览`
推进；成果章内部按 `问题与方案 → 方法/模型 → 验证 → 机制解释 → 小结` 推进。短报告各取主干即可。

## 2. 视觉规格

固定值，不要临场发挥。颜色写 hex 不带 `#`。

| 角色 | 值 |
|---|---|
| 中文字体 | 微软雅黑（deck 里 `Microsoft YaHei` 是同一字体的另一写法，**新写的一律用「微软雅黑」**，混用会出现同页字重不一致） |
| 西文 / 公式 / 引文 | Times New Roman |
| 封面副标题（"博士学位论文答辩"） | KaiTi |
| 主蓝（章节名、封面主标题） | `003399` |
| 断言蓝（断言句） | `002060` |
| 强调红（断言句里的关键短语、创新点标签、图上数字标注） | `C00000` |
| 流程图框字 | `00448D` / `2A5697` |
| 第二条主线（与蓝线并列的另一路研究） | `407523` / `4C8232` |
| 底色 | 纯白；旗标与页码徽章为深蓝填充白字 |

字号阶梯（pt，16:9 / 13.33×7.5 in）：

| 元素 | 字号 |
|---|---|
| 封面主标题 | 54 |
| 封面副标题 / 落款 / 日期 | 24 |
| 目录「目录」二字 | 54 |
| 页标题：章节名 / 页主题 / 章节号 | 28 / 24 / 22 |
| 页顶断言横幅 | 24（矮变体 20） |
| 面板旗标 | 20 |
| 总结页 ✓ 结论行 | 20 |
| 图注、小标签、参数标注 | 12–14 |
| 文献引文行 | 10–10.5 |
| 页码 | 14 |

关键几何（英寸，左上角原点）。面板是起点值，按 §3.2 随图调整：

| 构件 | 位置与尺寸 |
|---|---|
| 章节号（深蓝平行四边形内） | (0.38, 0.25) 0.77×0.47 |
| 章节名 | (1.19, 0.17) 高 0.57 |
| 标题分隔竖线 | (2.39, 0.29) 长 0.34 |
| 页主题 | (2.39, 0.21) 高 0.50 |
| 页顶断言横幅 | (0.37, 1.03) 12.60×0.98；矮变体 (0.37, 0.93) 12.39×0.52 |
| 双面板 | 左 (0.38, 2.43) 6.06×3.81 ｜ 右 (6.70, 2.43) 6.12×3.81；删掉 ✓ 行后下沿可延到 6.95 |
| 三面板 | (0.06 / 4.81 / 9.08, 1.16) 宽 4.70 / 4.20 / 4.02，高 6.30 |
| 面板旗标 | 骑在面板顶边上，y = 面板顶 − 0.22，宽 3.35–3.93，高 0.46 |
| 侧栏断言（图阵页专用） | (10.65, 1.03) 宽 2.30，高度拉满到 6.95 |
| 总结页 ✓ 结论行 | (3.0, 6.36) 约 7.5×0.96 |
| 文献引文行 | y = 7.24，高 0.27 |
| 页码徽章 | (12.40, 7.16) 1.13×0.34 |

## 3. 单页法则

### 3.1 文字层（Assertion–Evidence 的中式答辩变体）

该样板与英文 A–E 的差别，是把 message title 拆成了**两层**：标题栏只做导航，断言句另立横幅。
每页必须同时具备——

1. **标题栏**：`章节号 ｜ 章节名 ｜ 页主题`。这是导航，写主题标签即可，**不要**写成结论句。
2. **断言句**：一句，只出现一次——常规页放**页顶横幅**（做了什么 + 得到什么结论，关键短语标红），
   图阵页放**侧栏**。**内容页不要页底 ✓ 行**，克隆版式 6 后把它删掉，腾出的高度给证据；✓ 行只用在总结页。
   想强调某个数字，**标在图上**：红色箭头或圈 + 数字，贴着对应的数据点，而不是在页底再写一行。
3. **证据**：图 / 表 / 公式占据中部 ≥60% 版面。一个面板 = 一个 exhibit，面板旗标写这个
   exhibit 是什么。图下灰条只在需要区分子图时用，不要和旗标说同一件事。
4. **引文**：凡用他人图，页底 10pt 行写 `作者 (年份) 期刊`，一图一条；引文行上方不要有文字压着。

**信息密度上限：每页 ≤3 个核心信息。**超了就拆页。被砍掉的细节写进演讲者备注，不要塞回版面
——备注是信息的去处，不是垃圾桶，每页都要有口播稿。

**量化成果前置**：有数就把数写进断言句（"粘附能提高 3.4 倍"胜过"粘附能显著提高"）。

### 3.2 证据区排版

- **入页前处理图**：先跑 `scripts/trim_figure.py -input <图或目录>` 裁白边（另存 `*-trim.png`）；
  版式自己编 (a)(b) 的页，要裁掉或盖掉原图自带的子图字母；放大检查裁剪边缘有没有残留的竖线、点。
- **面板随图定尺寸**：图的宽或高至少撑满面板内框的 90%。被高度卡住时收窄面板，把省下的宽度让给
  另一面板——两面板宽度比 ≈ 两图宽高比之比，中缝约 0.26 in，旗标跟着面板居中。`qa_check.py`
  的填充门会报没撑满的图。
- **图内字投影可读**：缩到页面上后，图内最小的字要有 ~10 pt。8 个子图挤一张的大图，只取与断言相关的
  1–2 个子图，其余进备注。
- **图阵页（版式 7）只用于 ≥ 6 张子图**，侧栏高度拉满，不再克隆第二个框放数字（数字进断言或标在图上）。
  ≤ 4 张子图用版式 6 去掉面板框：顶幅 + 2×2 或 1×4 满宽排布。
- **窄框（< 3.5 in）文字一律左对齐**。样板侧栏是两端对齐，中英混排的短行会被拉出大空隙。

### 3.3 公式与符号

- 变量（a、E、γ、ε、σ）用 Times New Roman **斜体**；描述性下标（usf、Slab、FCC）、数字、单位、括号用正体。
- 上下标用 run 的 `subscript` / `superscript` 属性，**不用 Unicode 上下标字符**（₀、ᵢ 只覆盖少数字符，
  `usf` 就写不出来，和真下标混排还会大小不一），`qa_check.py` 会报。单位里的 ² ³ 可以保留。
- `∥ ⊥` 设 Times New Roman，微软雅黑里 ∥ 会显示成 "ll"。
- officecli 做法：先写纯文本，再在该页范围内用 `--find` 设格式（会自动拆 run）：

```powershell
officecli set deck.pptx '/slide[7]' --find 'a(?=∥)' --prop regex=true --prop italic=true --prop 'font=Times New Roman'
officecli set deck.pptx '/slide[7]' --find '∥' --prop subscript=true --prop 'font=Times New Roman'
```

## 4. 版式表与克隆

`assets/reference-deck.pptx` 的 10 页即 10 种版式。它是**填好内容的样板**不是空模板——
克隆后逐一替换文本，残留的样板词由质检门兜底。

| 页 | 版式 | 用途 |
|---|---|---|
| 1 | 封面 | 标题 / 汇报类型 / 答辩人 / 导师 / 单位 / 日期 |
| 2 | 目录 | 6 条编号条目，左侧竖排「目录」（仅长答辩；条目全部同色，不高亮任何一条） |
| 3 | 三面板 + 旗标 + 逐图引文 | 文献综述、现有理论对比 |
| 4 | 流程总览（多行章节框 + 箭头链 + 双色主线） | 本文主要研究内容 |
| 5 | 章节扉页 | 目录版式的高亮变体，当前章加深其余淡出（仅长答辩） |
| 6 | 顶幅断言 + 双面板（删掉页底 ✓ 行） | **内容页主力版式**；短报告的 take-home 总结页也用它（顶幅写结论 + 一张最关键的图） |
| 7 | 图阵（(a)–(h) 编号）+ 右侧竖排断言 | ≥ 6 张子图的结果页 |
| 8 | 四栏总结（每栏 ✓ 结论 + 底部创新点标签） | 长答辩总结页 |
| 9 | 纯文本列表页（顶部横线 + 编号条目） | 读博期间主要成果、参考文献 |
| 10 | 致谢/结束页 | 「敬请各位老师批评指正！」 |

克隆、填内容、后续修改一律用 officecli（命令手册 `references/officecli.md`，属性名查
`officecli help pptx <element>`）。下文 `<skill>` 指本 skill 所在目录。

```powershell
Copy-Item <skill>/assets/reference-deck.pptx <工作目录>/deck.pptx
# 1. 先剥掉动画（样板只有第 6 页有；其余页报 matched no elements 属正常）
1..10 | % { officecli raw-set deck.pptx "/slide[$_]" --xpath '//p:timing' --action remove }
# 2. 按版式计划逐页追加克隆，再删掉原 10 页
foreach ($k in 1,2,5,3,4,6,6,7,5,6,8,9,10) { officecli add deck.pptx / --from "/slide[$k]" }
1..10 | % { officecli remove deck.pptx '/slide[1]' }
```

填内容：先 `officecli get deck.pptx '/slide[N]' --depth 3` 看结构，再把整份文案写成一个
batch JSON，用 `officecli batch deck.pptx --input fill.json --json` 执行。批处理是原子的，
任一条失败整批回滚。改文案只改这份 JSON。替换样板词会保留 run 格式，组合形状里的文字也能改到：

```json
[{"command":"set","path":"/slide[4]","props":{"find":"<样板句>","replace":"<新断言句>"}}]
```

## 5. 质检门（每轮必过）

```bash
# 程序化：越界 / 溢出 / 样板残留词 / 数字与语料交叉核验 / 版面（重叠、图未撑满面板、空白区、Unicode 上下标、窄框两端对齐）
~/mysoft/env/pyenv/codex/Scripts/python <skill>/scripts/qa_check.py \
    -deck <工作目录>/deck.pptx -source <内容语料.md>
```

```powershell
# COM 渲染导出 PNG，逐页目检
& <skill>/scripts/export_png.ps1 -path_deck <工作目录>/deck.pptx -path_out <工作目录>/qa
```

- ❌ 必须修完。⚠️ 是粗估：逐条修掉，或在交付说明里写明为何保留。
- **数字交叉核验**会把页面上语料里查不到的数据型数字全部列出来。这一关拦的是最贵的错误——
  答辩现场被问到一个自己编出来的数。
- **PNG 目检按清单逐页作答**（Read 工具读图），答案表写进交付说明；任何一项为「否」先修再交付，
  不要只写一句"已看过、已修完"：

| # | 问题 |
|---|---|
| 1 | 图是否撑满面板，左右或上下没有超过面板 10% 的空带？ |
| 2 | 整页没有空着的一角（如侧栏只占上半、右下全空）？ |
| 3 | 文字没有压到别的文字、图或引文行？ |
| 4 | 原图子图字母、裁剪脏边已清掉？ |
| 5 | 公式变量斜体、上下标正确？ |
| 6 | 图内最小的字投影能看清？ |
| 7 | 断言只出现一次，而且和图讲的是同一件事？ |

交付前再手过两项：Ghost deck 测试（§1），以及每页备注是否都有口播稿。

## 6. 陷阱

- **officecli 不一定在 PATH**：Windows 上装在 `$env:LOCALAPPDATA\OfficeCLI\officecli.exe`。找不到命令时
  先 `$env:Path += ";$env:LOCALAPPDATA\OfficeCLI"`（git-bash 用 `~/AppData/Local/OfficeCLI/officecli.exe`）。
- **`--find` 配 `baseline=sub` 报成功但不写入**（v1.0.152 实测）。上下标一律用 `subscript=true` /
  `superscript=true`。
- **带 `<p:timing>` 的页不能直接用 officecli 复制或删形状**（v1.0.152 实测）：复制页会给形状
  换 id，但动画还指向旧 id；删形状会留下空的动画节点。`validate` 报错，PowerPoint 拒绝打开。
  所以 §4 第 1 步先剥动画。
- **新建中文文本要带 `--prop lang=zh-CN`**（v1.0.152 实测）：`add --type textbox` 和带文字的 shape
  默认写 `lang="en-US"`，PowerPoint 就不按中文避头尾断行，行首会出现「，」「）」。克隆来的样板文字
  本身是 zh-CN，不受影响。
- **克隆后形状 id 全部改变**，不能沿用参考 deck 里的 `@id=`，每次都先 `get` 查当前路径。
- **`--find` 不限定页时会替换全 deck**。克隆页上的样板文字完全相同，路径要写到
  `'/slide[N]'`，不要写 `/`。
- **页码是 `slidenum` 域加上 `" / 46"` 这个文本 run**。页号会自动跟随，总页数变了只需改后半段：
  `officecli set deck.pptx / --find '/ 46' --replace '/ <总页数>'`。
- **PowerShell 5.1 按 GBK 解析无 BOM 的 .ps1**，中文路径会乱码。工作目录用 ASCII 路径，
  或给 .ps1 加 BOM。
- **git-bash 内联 PowerShell 会吃掉 `$`**：一律 `PowerShell` 工具直接跑，或 `-File` 执行脚本，
  不要在 bash 里拼 `powershell -Command`。此外 git-bash 里没有 `powershell.exe`，用
  `PowerShell` 工具或 `& <脚本路径>`。
- **officecli 会常驻后台占着文件**（空闲 60 s 后自动释放）。跑 `qa_check.py`、`export_png.ps1`
  或用 PowerPoint 打开之前，先 `officecli close <deck>`，再 `officecli validate <deck>`。
- **不要加载 officecli 自带的设计 skill**（`officecli load_skill pptx` / `pitch-deck` / `morph-ppt`）。
  版式和配色一律按本 skill §2，officecli 只用来执行编辑命令。
- **officecli 的属性名不要猜**，查 `officecli help pptx <element>`；`view screenshot` 用的是
  officecli 自己的渲染器，不能替代 §5 的 COM 导出。
- **COM 的 `Presentations.Open` 要绝对路径**，相对路径静默打开失败。
- **PowerPoint COM 是单实例**：用户开着 PPT 时脚本拿到的是同一个进程。只 `Close()` 自己打开的演示文稿，
  没有别的演示文稿时才 `Quit()`（`export_png.ps1` 已按此处理），否则会连用户的文件一起关掉。

## 7. 交付

- 工作 deck 与 PNG 放项目目录，不放 skill 目录。
- 过程/汇总文档放 `<project-root>/ai-guide/<子目录>/<YYYYMMDD>-<agent>-<subject>.md`，附 §5 目检答案表。
- 交付时明说：哪几页的图是占位待补、哪几处断言句需要作者本人复核。
